const registration_button = document.getElementsByClassName('registration_link')[0];

registration_button.addEventListener("click", ()=> {
    document.getElementById("window-alert").style.display = "block";
})